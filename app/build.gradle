import java.util.Base64

plugins {
	id 'com.android.application'
}

def getSwvProperties() {
	def properties = new Properties()
	def propertiesFile = file('src/main/assets/swv.properties')
	if (propertiesFile.exists()) {
		properties.load(propertiesFile.newDataInputStream())
	} else {
		throw new GradleException("swv.properties file not found in app/src/main/assets/")
	}
	return properties
}
def swvProps = getSwvProperties()

android {
	namespace swvProps.getProperty('build.application.id', 'cz.jankoran.jellyfinuploader')
	compileSdk swvProps.getProperty('build.sdk.compile', '36').toInteger()

	defaultConfig {
		applicationId swvProps.getProperty('build.application.id', 'cz.jankoran.jellyfinuploader')
		minSdk swvProps.getProperty('build.sdk.min', '24').toInteger()
		targetSdk swvProps.getProperty('build.sdk.compile', '36').toInteger()
		versionCode swvProps.getProperty('build.version.code', '1').toInteger()
		versionName swvProps.getProperty('build.version.name', '1.0.0')
		multiDexEnabled true
	}

	signingConfigs {
		release {
			def signingKey = System.getenv("SIGNING_KEY")
			if (signingKey != null) {
				def keystoreFile = File.createTempFile("release", ".jks")
				keystoreFile.bytes = Base64.decoder.decode(signingKey)
				keystoreFile.deleteOnExit()
				storeFile keystoreFile
				storePassword System.getenv("KEY_STORE_PASSWORD")
				keyAlias System.getenv("KEY_ALIAS")
				keyPassword System.getenv("KEY_PASSWORD")
			}
		}
	}

	buildTypes {
		release {
			minifyEnabled false
			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
			def signingKey = System.getenv("SIGNING_KEY")
			if (signingKey != null) {
				signingConfig signingConfigs.release
			}
		}
	}

	buildFeatures {
		buildConfig true
	}

	compileOptions {
		sourceCompatibility JavaVersion.VERSION_17
		targetCompatibility JavaVersion.VERSION_17
	}
}

dependencies {
	testImplementation 'junit:junit:4.13.2'
	androidTestImplementation 'androidx.test.espresso:espresso-core:3.7.0'

	implementation fileTree(include: ['*.jar'], dir: 'libs')

	implementation 'androidx.multidex:multidex:2.0.1'

	// Firebase (no google-services.json = not configured, but needed for compilation)
	implementation 'com.google.firebase:firebase-auth:24.1.0'
	implementation 'com.google.firebase:firebase-config:23.1.0'
	implementation 'com.google.firebase:firebase-messaging:25.0.2'
	implementation 'com.google.firebase:firebase-analytics:23.2.0'

	implementation 'com.google.android.gms:play-services-auth:21.5.1'
	implementation 'com.google.android.gms:play-services-ads:24.9.0'
	implementation 'com.google.android.gms:play-services-location:21.3.0'

	implementation 'com.google.android.material:material:1.14.0'

	implementation 'androidx.core:core-splashscreen:1.2.0'
	implementation 'androidx.constraintlayout:constraintlayout:2.2.1'
	implementation 'androidx.swiperefreshlayout:swiperefreshlayout:1.2.0'
	implementation 'androidx.appcompat:appcompat:1.7.1'
	implementation 'androidx.browser:browser:1.10.0'
	implementation 'androidx.work:work-runtime:2.11.2'

	// Plugins
	implementation 'com.journeyapps:zxing-android-embedded:4.3.0'
	implementation 'androidx.biometric:biometric:1.1.0'
}

if (file('google-services.json').exists()) {
    apply plugin: 'com.google.gms.google-services'
} else {
    println "swv-fcm-sync: google-services.json not found. Skipping Firebase Configuration."
}
